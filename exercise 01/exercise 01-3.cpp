#include <iostream>
#include <cmath>

int main(){
    int n = 0;
    double a = 0;
    a = pow(n,2);
    std::cout << a << '\n';
    n++;
    a = pow(n,2);
    std::cout << a << '\n';
    n++;
    a = pow(n,2);
    std::cout << a << '\n';
    n++;
    a = pow(n,2);
    std::cout << a << '\n';

    return 0;
}